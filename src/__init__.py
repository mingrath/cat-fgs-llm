"""cat-fgs-llm — binary-plus-wrapper feline pain decision support.

The DINOv2 + CORN engine (src.model) is conceded plumbing, never claimed novel.
The headline contribution is the capture-condition confound-attribution protocol
(src.eval.confound) — a one-directional, power-aware audit. The VLM-as-AU-rater
kappa protocol (src.vlm + src.eval.kappa) is a guarded, inspected-not-validated
reliability check ranked below it (result pending an independent vet anchor; kept
as kill-tree insurance). The welfare wrapper (src.wrapper) is cited supporting
plumbing, not a headline.
"""
