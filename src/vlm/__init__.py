"""Claude 5-AU weak-labeler (Batches API, cached rubric, enum [0,1,2] schema).

The VLM emits ONLY the 5 AU atoms in {0,1,2}. The 0-10 sum and the 0.39 flag
are computed in code, never by the VLM.
"""
