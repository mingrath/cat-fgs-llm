"""cat-fgs-llm — binary-plus-wrapper feline pain decision support.

The DINOv2 + CORN engine (src.model) is conceded plumbing, never claimed novel.
The headline contributions are two portable methods: the VLM-as-AU-rater kappa
protocol (src.vlm + src.eval.kappa) and the capture-condition confound-attribution
protocol (src.eval.confound).
"""
